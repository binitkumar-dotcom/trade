# Restored Jobs Log App

A simple Streamlit app to manage and download restored job logs.

## Features
- View job logs in a table
- Add new job entries
- Download CSV file
- Auto-create log file if missing

## How to Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
